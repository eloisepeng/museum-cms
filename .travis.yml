language: node_js
node_js:
- 12.12.0
cache:
  directories:
  - node_modules
script:
- npm install
- scripts/build_check.sh
deploy:
  provider: pages
  skip_cleanup: true
  github_token: "$GITHUB_TOKEN"
  keep_history: true
  on:
    branch: master